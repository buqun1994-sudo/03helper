# Use this field layout for repository-external staging or production signing.properties files.
# Never commit real passwords, signing.properties, or the keystore file.
storeFile=03helper-release.jks
storePassword=replace-me
keyAlias=03helper-release
keyPassword=replace-me
