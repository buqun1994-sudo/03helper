# 2026-08-24 维护与云端配置施工目标

> 本文件保存本轮用户原始需求，作为上下文压缩后的持续目标基线。代码、测试和文档变更必须逐项回看本文件；未完成项不得写成已完成。

## 2026-09-04 架构决策更新

管理已安装应用的最终口径已由本轮确认的全量用户应用主链取代下方旧决策：通过 03桌面协议 v2 只读桥接返回当前用户的全部非系统应用，按首次安装时间倒序展示，以精确 `packageName` 作为行和操作身份，并提供启动、强停、清数据、一键授权、卸载、详情六项 typed action。该库存只映射管理页，不污染受信安装 manifest、安装选择或批量修复授权。

## 2026-09-02 架构决策更新

原始需求中的“全部更新”是当时的探索口径，现由本轮确认的产品与架构决策取代：检查更新页不暴露批量按钮，只有有更新的应用行可点击并冻结为单目标批次。车机组件更新与首次安装共享同一制品准备 / 设备安装底层主链，更新通过 `REINSTALL_SELECTED` 只覆盖当前目标；`desktop` 只能作为必要的内部 reusable 前置，不能出现在当前目标结果页。助手自更新共享制品准备主链，准备完成后由 Android 系统安装器承接并以 `PackageManager` 回读确认。以下“用户原文”保留作历史输入，不再作为现行 UI 或状态机口径。

本轮目标模式补充决策（历史，已被 2026-09-04 口径取代）：管理已安装应用恢复为仅展示签名配置 / 已验证 manifest 中声明的受控组件，不再枚举或展示车机上的其它第三方包。受控应用的启动、强停、卸载确认和详情四项操作继续保留，并统一经过当前受控身份校验；列表阶段只读取轻量包管理器 / `dumpsys` 元数据，不调用 `pm list packages --user 0 -3`，不为进入页面传输 APK。

## 用户原文（原样保存）

云端配置：
1、现在，云端配置允许加入03车机助手自身的配置信息，我们的蓝奏云链接里面也会放入03车机助手的压缩包。
2、客户端的部分在初次安装流程中要主动忽略掉自身的压缩包，给用户展示的选项还是其他的应用。

车机连接：
1、现在有bug，点击断开车机连接按钮后预期是按钮变成“连接”，但现在居然是“正在连接”的加载状态，但也不连，纯粹是状态机错误

维护页所有二级页：
之前实现得太糙了，有很明显的UI错误。
1、返回按钮UI明显错误，套了个框，位置也不对
2、每一页都多了一句多余的“下一步：XXXXX”，删掉
3、每一页都多了一句多余的“本次操作：XXXXX”，删掉
4、每一页顶部都有标题了，紧接着下面又是标题的icon，然后又是一个简介，应当是标题就应该是“icon+标题”的样式，原本icon、简介删掉。
5、从第2页返回时，应当返回第1页之前的滚动位置，而不是固定返回到头部。这个功能适用于所有应当有返回的地方。
6、其余UI交互根据下面的内容来，没写的就是下个阶段再改

检查更新：
1、检查更新的目的是分别检查已安装应用和自身的版本更新。点击后，向云端配置获取所有的版本号并跟目前车机安装的我们的APP和03车机助手自身进行比对，UI上要分开来两个板块，先是自身后是其他APP。
①检查更新需要连接车机，如果断开了就只检查03车机助手自身是否有更新，其他APP的部分不显示APP，只提示车机未连接，如果环境一切正常就在每个APP（包括自身）后面显示有无更新的状态，
②检查有更新后，底部按钮“全部更新”，点击后复用“初始化安装”的整个流程和UI（但要根据情况修改文案和隐藏UI），分别下载并覆盖安装后检查车机APP，然后结束，点击完成后回到维护页。

2、重新安装，删掉

3、修复授权：
①修复授权功能的意义在于，用户有可能因为某些原因导致应用的授权失效了，重新完成授权。进入页面后（如果车机断了就显示未连接车机，底部“确认”按钮，点击后反回维护页，让用户去），第一步应当是自动检查车机应用的授权状态，页面应当是展示每个应用，展示logo、应用名称、版本号等静态信息，和当前检查到哪项授权以及最终检查结果的动态信息
②检查完成后，如果都正常，底部显示“检查完成，所有应用授权正常”和“重新授权”按钮，点击后向车机重新授权一次，页面动态展示授权过程，完成后底部显示“授权完成，所有应用授权正常”和“重新授权”按钮，用户可以无限重复这个动作，或者返回上一页。
如果检查有没授权的项目，底部就不显示文案，只有“重新授权”按钮，点击后还是上面的流程

4、管理已安装应用：
①这个功能的意义在于，给用户对应用的常见操作增加一个统一的入口，像“启动”、“强行停止”这种按钮都应该汇集在这个功能里面。
②点击后打开第二页，页面首先扫描和加载当前车机的每个应用，每个应用仍然像授权页面那样展示静态信息，还有4个按钮，分别是启动应用、强行停止、卸载应用和应用详情。
③点击启动应用/强行停止，弹出一个小的浮窗提示结果就好了，比如“启动成功”、“启动失败”这样的；
④点击卸载应用，弹窗询问（标题是卸载应用，内容是APP名称，两个按钮是确定和取消），确认则进入第三页，顶部是应用名称的标题，以及相关信息，你觉得能写什么就写什么，动态展示卸载的状态。卸载后，底部按钮显示为“卸载完成”，点击后返回应用管理页面，页面重新加载展示已安装的应用；
⑤点击应用详情进入第3页，顶部应用详情标题，内容区左右分布显示应用名、包名、版本号、文件大小、安装时间、更新时间、文件路径、UID。（反正常见应用详情里，你觉得能写啥，你就堆在这里面。）

5、安装文件管理器
①改名：安装应用。这个作用是让用户重新走一遍安装流程。整体仍然是初始化安装的那一套。
②点击后进入第二页，获取链接内除了自身之外的所有应用，仍然是
像初始化安装界面那样，需包含以下内容：
1. 静态信息：显示 Logo、应用名称、版本号、文件大小，勾选按钮
2. 状态信息：显示“已安装”和“未安装”状态。
   (a) 已安装的默认勾选。
   (b) 未安装的用户可以勾选。
3. 底部按钮：显示为“开始下载并安装”。
③点击“开始下载并安装”按钮，之后复用初始化安装的流程。页面内该调整的文案和 UI 由你来把控，完成后回到维护页面。

6、删除启动03歌词、启动03桌面

7、一键清理，删掉，但相应的，我们在所有的安装行为之后都应该自动删除缓存，这是默认行为，不应该由用户来手动选择。

8、导出诊断，如果我们的日志系统足够丰富，信息足够多，足够用来进行人工的排查，那这个功能就保留，反之则删掉。
①如果保留按照上面的信息，就是按照上面的修改方式，重新调整第二页的布局。
②
1. 内容区：应当展示日志文件的加载状态。加载完成之后，显示日志文件的大小、日期等相关信息。
2. 底部按钮：为“导出诊断文件”。点击之后触发系统的分享页面。安卓机一般都有这个通用的分享功能吧？如果没有，就做成跳转到文件管理的默认应用，选择某个位置，然后点击保存。

## 交付纪律

1. 云端配置、自身压缩包过滤、断开连接状态机、维护页路由与页面状态必须沿既有领域 / 应用 / UI owner 修改。
2. 每个完成的行为都要有对应的编译或自动化验证；涉及界面的改动必须继续做真实运行级 smoke，环境阻断要明确记录。
3. 本文件只追加客观进度，不删除或改写上面的用户原文。

## 当前状态

- 需求基线：已保存；本轮代码施工、手机页面验证和真实车机安装 / 授权最小复测完成，维护全量动作仍待主测。
- 云端自身条目过滤：已实现并完成代码验证；真实 Cloud 发布资料仍待外部主测。
- 手动断开后按钮回到“连接”：已实现并完成状态机回归。
- 维护二级页布局、返回滚动位置和路由：已实现并完成手机页面检查。
- 检查更新、修复授权、应用管理、安装应用：已接入现有主链并完成手机页面检查；诊断入口按当前日志能力隐藏。

## 客观进度追加（施工中）

- 已完成基线提交：`13ad8ca chore: checkpoint package and release updates`；本轮后续改动均以该提交为起点，未提交密钥、设备信息或构建产物。
- 已完成：云端 `03helper` 自身条目身份、首次安装入口过滤、蓝奏云自身包名强校验、手动断开状态机回归；相关 Kotlin 编译与既有单测通过。
- 已完成：维护页纯图标返回、标题图标合并、移除页面多余提示、首页滚动位置保留；维护首页隐藏重新安装、启动 03 歌词 / 桌面、一键清理和诊断入口。
- 已完成：检查更新分为助手自身 / 车机应用两块；断开时只保留自身版本信息；新增授权检查进度、受控应用启动 / 强停 / 卸载 / 详情结构化契约与页面；“安装应用”拥有选择页并复用既有安装主链；安装运行结束自动清理助手私有缓存。
- 已完成本轮 UI 收口：更新行按状态显示 `当前 vX` 或绿色目标版本 `vX`，不再显示远端 / 当前双版本；断开车机时底部更新判断只看助手自身；应用管理四个操作按钮统一使用居中组件，未安装应用的不可用按钮有明确禁用态。
- 当前验证（车机复测前的历史记录）：`:app:compileDebugKotlin`、`:app:compileDebugAndroidTestKotlin`、`:app:testDebugUnitTest`、`:app:lintDebug`、`:app:assembleDebug`、`:app:assembleDebugAndroidTest` 和 `:app:connectedDebugAndroidTest` 已通过；测试手机页面已实测更新页（已连接 / 断开）和应用管理页。该段当时尚未执行真实车机写入，最新车机安装 / 授权结果见下方 2026-08-28 追加记录。

## 2026-08-28 客观进度追加：真实车机安装 / 授权最小复测通过

- 用户明确授权清理上次测试状态后，已定向卸载四个受控包、撤销旧授权并重置对应 AppOps，撤销文件管理器存储权限；第三方 `com.mengbo.monitor` 无障碍条目保留。
- 目标车机 `S56_HQX`（Android 9）重新上线。连续执行 03桌面 + 03歌词、单独 03投屏、单独文件管理器三组批次，助手页面均显示“安装完成 / 安装成功”；车机包存在，03投屏为零授权动作，文件管理器存储权限回读为 `granted=true`。
- 授权页四个受控应用均显示“授权正常”；点击“重新授权”后显示“授权完成，所有应用授权正常”；再次进入安装应用页四项均为“已安装”，底部为“知道了”，没有旧批次失败残留。
- 该证据覆盖此前两个真实 UI 误报；Cloud / 下载源、维护断线恢复、冷启动和受控应用管理仍按验证矩阵保留为独立主测范围。
