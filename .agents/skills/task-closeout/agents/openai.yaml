interface:
  display_name: "任务收尾"
  short_description: "构建覆盖安装 Debug、执行快速 smoke、交付人工主测与提交门禁"
  default_prompt: "Use $task-closeout to build and preserve-data install the latest Debug APK, run only the bounded quick smoke, hand off complex manual tests, then enforce the commit gate."
