interface:
  display_name: "任务收尾"
  short_description: "完成安装助手收尾、授权验证、人工主测与提交门禁"
  default_prompt: "Use $task-closeout to run only user-authorized smoke, otherwise prepare exact manual tests, then enforce the commit gate."
